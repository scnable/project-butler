// TODO: complete the sample workflow
export function todoSample(): void {
  // FIXME: replace the placeholder implementation
  const DEBUG_MODE = false;
  if (DEBUG_MODE) {
    console.log('DEBUG is an identifier and string, not a comment marker');
  }
}
