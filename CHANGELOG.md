# 更新记录

## 0.7.5（预览测试版）

- 在资源管理器新增“已打开文件目录”，基于当前标签构建编辑器组、工作区和文件夹层级，不扫描项目文件。
- 文件节点复用并聚焦已有标签；工作区外普通文件统一进入尾部分组，Diff 等特殊编辑器不伪装为文件。
- 目录默认展开，右键支持显式折叠/展开，标题栏支持全部展开/折叠；补齐 `TreeView.reveal` 所需父节点索引与失败降级。
- 新增“目录树/VS Code 原生”视图选择，首次互斥需确认，并对 `explorer.openEditors.visible` 采用有条件安全恢复。
- 修复新版 VS Code 将 `explorer.openEditors.visible` 最小值限制为 `1` 时，点击“隐藏原生打开的编辑器”没有反馈的问题；现在会说明公开 API 限制并给出手动隐藏位置。
- 标签整理改为稳定地将非项目标签移到末尾；项目内标签保持用户顺序，不再按文件夹重排或着色。
- 旧 `autoOrganize` 字段继续兼容读取，但配置名称、说明和手动命令均改为新语义。
- 修复自动整理与 VS Code 激活标签更新之间的竞态：前台打开的外部文件移动到末尾后仍保持活动；用户快速切回其他文件时也不会被整理任务抢回焦点。
- 隐藏原生“打开的编辑器”不再尝试写入模式不允许的 `explorer.openEditors.visible = 0`，避免 VS Code 自己在右下角额外显示配置错误；手动隐藏说明统一使用居中模态弹窗。
- 修正原生视图互斥实现：优先调用 VS Code 注册的 `removeView/open` 命令真正隐藏与恢复“打开的编辑器”；所有确认入口统一使用居中模态弹窗，扩展启动时不主动打扰用户，缺少命令时才进入手动降级。
- 配置栏合并重复的“当前集合功能”和“个人功能默认值”为“当前生效功能”，直接显示实际值及其来源；集合外项目由同一入口编辑个人默认。
- 集合功能新增“跟随个人默认”，新建集合不再固化标签与大纲默认值；内部存储升级到 v2，旧 v1 数据保持兼容。
- 项目集合迁移快照升级到 v2，导入导出会保留集合继承语义；旧 v1 快照缺失字段仍按旧版安全默认迁移。
- 类型检查、110 项单元测试、185 项源码 Extension Host 测试和 3 项安装态测试通过。

## 0.7.4（预览测试版）

- 增强函数大纲为 C/C++ 条件编译生成可折叠区域和分支节点，并忽略注释中的伪指令。
- 保守归并语言服务返回的结构体首尾重复项，保留成员且不合并前置声明或不同容器符号。
- 点击大纲项后聚焦源码、选中符号名称，并显示约 1.2 秒的主题兼容整行定位提示。
- 大纲固定使用树状层级；大纲栏只保留范围、排序、搜索、刷新和设置入口，低频显示项统一放入配置栏。
- 新增 VS Code 能力检测：未注册 `chat.disableAIFeatures` 时显示需要 1.104+ 并阻止写入；依赖的原生命令在调用前检查是否存在。
- 类型检查、90 项单元测试、175 项源码 Extension Host 测试和 3 项安装态测试通过。
- 本版本定位为持续更新的预览测试版，真实 C/C++ 工程中的配色、折叠与跳转视觉效果仍需人工验收。

## 0.7.3（预览测试版）

- 项目集合与项目条目新增右键维护操作：重命名集合、重命名项目别名、重新选择项目路径和从当前集合移出。
- 集合维护统一使用稳定 ID，路径变更和成员移出会同步处理项目绑定并即时重算当前项目归属；所有操作都不会改动磁盘项目文件。
- 增强函数大纲能够识别 `files.exclude` 导致的符号缺失，显示实际屏蔽规则和“管理/取消屏蔽”入口，不再误报缺少语言扩展。
- 资源屏蔽确认增加语言符号风险说明，取消 `files.exclude` 后增强大纲会立即重新检测。
- 建立候选 VSIX 的独立安装态测试配置，并补充 `0.7.3` 问题修订、统一标识、代码 TODO 和 `1.0.0` 路线图文档。
- 类型检查、79 项单元测试、169 项源码 Extension Host 测试和 3 项安装态测试通过。
- 本版本定位为持续更新的预览测试版，不代表 `1.0.0` 功能冻结或正式发布承诺。

## 0.7.2

- 配置侧栏新增“VS Code 内置 AI 功能”分组和“开启/完全关闭”开关，直接修改用户级 `chat.disableAIFeatures`。
- 完全关闭前显示影响范围确认，明确会隐藏内置 Chat、禁用内联 AI 功能及 Copilot 扩展。
- 明确第三方边界：拥有独立界面的 Agent 扩展通常不会被直接禁用，依赖内置 Chat 的能力将不可用。
- 外部修改 `chat.disableAIFeatures` 后，配置侧栏状态会立即刷新。
- TypeScript 检查和 75 项自动测试通过。
- 建立真实 VS Code Extension Host 集成测试，覆盖扩展激活、核心命令与视图注册、源码打开、大纲刷新，以及 AI 用户设置写入和工作区覆盖识别；首批 3 项测试通过。

## 0.7.1

- 修复从侧栏开启标签自动整理后没有立即整理的问题。
- 初始化保护期内恢复或修改集合开关时，不再丢失首次整理请求；初始化完成后检查全部编辑器组。
- 开启开关后对全部编辑器组登记完整整理，不再只处理当前活动组。
- 活动标签是预览或固定文本时，立即整理同组其他可移动标签，同时保持该标签不动；设置页等特殊标签继续安全等待。
- TypeScript 检查和 75 项自动测试通过。

## 0.7.0

- 重建配置功能体系：项目集合和集合功能改由版本化内部存储管理，不再依赖活动集合文件。
- 新增独立“配置”侧栏视图，按集合功能、个人默认、项目操作、外部文件和函数大纲显示分层，选择后立即应用并持久化折叠状态。
- “添加项目”支持创建首个集合、选择其他集合或创建第二个集合，不再需要空模板。
- 新增集合选择、退出、导入和导出；导出文件只作为迁移快照，导入后不监听源文件。
- 导入采用字段级兼容，无效普通配置回退默认值；不可用项目路径支持基础目录重映射。
- 旧 `*.project-butler.json` 保留为一次性导入来源；`1.0.0` 前采用开发期宽松兼容策略。
- 新增内部集合存储与恢复优先级测试，自动测试增加到 75 项。

## 0.6.13

- 修复新建项目集合后点击函数大纲设置没有明确写入效果的问题；集合树中的大纲模式现在直接修改并保存 `features.symbolOutline.mode`。
- 将“函数大纲设置 → 大纲模式”重复两层收敛为一条“函数大纲模式”，并从项目集合标题栏隐藏低频的插件设置入口。
- 集合成员项目的大纲模式选择写入集合文件；集合外项目才写全局个人回退值。工作区显式设置仍可覆盖集合。
- 集合共享值优先于全局个人设置，避免历史全局选择继续遮蔽复制来的集合配置；标签自动整理采用相同优先级。
- 补充 v1、v2 集合升级与函数大纲字段写入，升级 v2 时同步迁移旧标签配置。

## 0.6.12

- 修复选择“仅原生”后，项目集合列表中的“函数大纲设置/大纲模式”点击没有有效恢复入口的问题。
- 两个列表入口现在直接打开三态大纲模式选择器；选择“仅增强”或“同时使用”后立即恢复并定位增强大纲。
- 集合配置文件仍保存可迁移默认值；列表点击修改的是本机显式模式，并在界面中标明配置来源。

## 0.6.11

- 移除函数大纲的固定模式和“跟随/已固定”按钮，大纲始终跟随当前活动文本文件。
- 设置页、插件详情等非文本编辑器不会替换最后一个源码文件的大纲。
- 旧版 `projectManager.symbolOutline.followActiveEditor` 配置和固定 URI 状态保留兼容但不再生效，不改写用户已有设置数据。

## 0.6.10

- 原生大纲隐藏提示新增“折叠/详情”状态，默认展开；折叠后仅保留一行必要说明。
- 展开或折叠状态在当前工作区固定保存，重载窗口及切换大纲模式后仍保持用户选择。
- 明确说明 VS Code API 无法自动隐藏或检测内置大纲的隐藏状态。

## 0.6.9

- 增强函数大纲默认回到“项目管家”插件侧栏，与项目集合和功能配置集中显示；仍可手动移动到辅助侧栏。
- 修复升级后 VS Code 恢复旧视图导致“位置未变化、内容空白”的问题：主视图会自动聚焦，资源管理器旧视图显示明确迁移入口。
- 项目集合模板新增 `features.symbolOutline.mode`，支持 `native`、`enhanced` 和 `both`，并补充 JSON Schema 校验与兼容默认值。
- 集合内项目会读取大纲模式默认值，本机显式设置优先；集合视图显示当前模式及配置来源。
- 切换大纲模式时同步更新视图可见上下文，避免启用增强大纲后首次定位发生时序问题。

## 0.6.7

- 增强函数大纲默认改为显示在资源管理器，不再放在“项目管家”项目集合界面中。
- 使用新的增强大纲视图标识，避免升级后继续沿用旧位置；旧位置贡献保留为隐藏兼容项。
- “仅增强”模式的原生大纲隐藏说明改为增强大纲内部常驻提示，不再依赖原生大纲标题栏警告。
- 常驻提示提供“定位原生大纲”和“改为同时使用”操作，明确 VS Code 不允许插件自动隐藏内置视图。
- 增强大纲仍可由用户手动移动到辅助侧栏，VS Code 继续保存用户布局。
- 新增仅增强模式常驻提示状态测试，自动化测试增加到 64 项。

## 0.6.6

- 标签自动整理改为按编辑器组保存待处理状态，打开文件后快速切换标签不再丢失整理请求。
- 自动整理从关闭切换为开启时，立即请求完整整理；当前为设置页、预览或固定标签时，切回普通文件后自动补偿执行。
- 同一事件中打开多个普通文件时逐个处理，并在整理后恢复用户原来的活动标签。
- 工作区外普通文件与项目文件使用相同的可靠触发链路；预览转普通标签后继续参与整理。
- 新打开的设置页、插件说明页等特殊标签如果未能立即移动，会在其再次激活时补偿移动到组末尾。
- 新增 4 项待整理状态测试，自动化测试增加到 63 项。

## 0.6.5

- 新增“仅原生”“仅增强”“同时使用”三种大纲模式，默认同时使用。
- “仅原生”会隐藏增强函数大纲；重新打开增强大纲时先询问新的使用模式，并继续复用同一视图。
- “仅增强”状态下，VS Code 原生大纲标题栏显示配置警告；关闭冲突提示而不选择时自动回退为“仅原生”。
- 新增“打开/定位增强函数大纲”“打开 VS Code 原生大纲”和“选择大纲模式”命令。
- 首次使用时提示增强大纲的默认位置、移动到右侧辅助侧栏的方法和位置保存行为。
- 新增 4 项大纲模式状态转换测试，自动化测试增加到 59 项。

## 0.6.3

- 新增“当前文件函数大纲”侧栏视图，读取 VS Code 语言服务提供的文档符号。
- 支持跟随/固定文件、树状/平铺、符号范围、源码/名称/类型排序和当前文件搜索。
- 支持点击跳转、光标所在函数同步高亮、函数行跨度、长函数和本次编辑状态提示。
- 新增跟随 VS Code、Source Insight 浅色和 Source Insight 黑色三种外观，并支持 90%～150% 缩放。
- 工作区外文件、集合外项目和无工作区文件均可使用函数大纲。
- 新增函数大纲领域模型测试，自动化测试增加到 55 项。
- 函数大纲顶部直接显示“外观”下拉框，并在视图标题栏增加颜色按钮，避免外观入口隐藏在低辨识度的更多菜单中。

## 0.5.0

- 项目集合视图同时显示集合 `name` 和真实配置文件名。
- 标签自动整理区分项目文件、工作区外文件和非项目编辑器；项目文件按目录成块，非项目标签尽量集中到末尾。
- 增加“屏蔽同类型文件（按扩展名）”，可将 `**/*.ext` 同时写入目录、搜索和文件监控设置。
- 工作区外文件改用 `‼` 徽标、红色系前景和错误色状态栏，提高辨识度。
- 明确 VS Code 公开 API 不支持标签边框、包围框、独立背景和粗体文字，不使用内部 CSS。

## 0.4.2

- 修复普通未关联工作区错误恢复全局最后项目集合的问题。
- 增加集合内项目、集合外项目和集合启动窗口的运行归属判断。
- 集合功能配置仅在当前工作区与集合项目精确匹配时生效，避免配置泄漏到无关项目。
- 集合外项目仍可加载和管理集合，但标签自动整理只读取本机显式覆盖或插件默认值。
- 集合视图新增当前项目归属节点，并显示集合配置是否作用于当前项目。

## 0.4.1

- 丰富项目集合视图图标，区分文件夹项目、VS Code 工作区、失效项目、集合配置和标签页操作。
- 自动整理开关使用动态状态图标：开启显示成功状态，关闭显示弱化状态。
- 为标签整理、资源屏蔽、屏蔽管理和外部文件查看命令补充语义图标。
- 图标统一使用 VS Code Codicon 和主题颜色，兼容明暗及高对比度主题。

## 0.4.0

- 标签页自动整理收敛为 `autoOrganize` 一个布尔开关，默认关闭；手动整理始终可用。
- 移除“查看已打开文件”命令、已打开文件数量展示、数量阈值和询问模式。
- 项目集合格式升级到 v3；v1、v2 继续兼容读取，修改标签配置时经确认升级。
- v2 的 `groupingMode: "auto"` 兼容映射为开启，`off` 和 `prompt` 映射为关闭。
- 开启自动整理后立即整理当前活动标签组，后续新普通标签继续增量归组。

## 0.3.2

- 修复打开或激活另一个集合文件时，当前活动集合被意外替换的问题。
- 活动集合确定后，只能通过新建、打开/切换或关闭命令改变；编辑当前集合仍会自动刷新。
- 启动恢复优先使用窗口已保存的集合，不再从多个已打开集合文档中任意选择。
- 标签归组补充处理预览标签转为普通标签的事件，转为可整理状态后立即重新判断阈值。
- 输出通道记录首次满足归组条件的模式、可整理数量和阈值。

## 0.3.1

- 修复修改集合归组方式或数量阈值后，不会立即检查当前标签组的问题。
- 标签设置分组同时显示全部普通文件数、当前组可整理文件数和触发阈值，避免把预览或固定标签误算为可整理文件。
- 配置变化未达到阈值或自动归组关闭时，在“项目管家”输出中记录明确原因。

## 0.3.0

- 项目集合格式升级到 v2，增加可迁移的标签页功能配置。
- v1 集合继续兼容读取，保存标签配置时经确认升级，不自动改写旧文件。
- 集合视图首项显示当前配置文件、版本和兼容状态，点击即可打开源文件。
- 标签配置可以在集合树中直接查看和修改，并显示本机覆盖、集合或默认值来源。
- 增加关闭当前集合、自动恢复控制，以及项目新窗口的集合关联。
- 未加载集合时直接显示创建、打开、字段和标签配置说明。

## 0.2.0

- 增加“查看已打开文件（按目录分组）”命令和标签右键入口。
- 快速选择器显示已打开普通文件总数以及每个目录的文件数。
- 增加按编辑器组计算的标签数量阈值，默认 8 个普通文件标签。
- 增加 `off`、`prompt`、`auto` 三种标签归组行为。
- 支持达到阈值后按直接父目录整理标签，并让后续新标签进入同目录块末尾。
- 增加“按文件夹整理当前标签组”命令和排序预览。
- 固定、预览和特殊标签作为安全边界，不参与自动移动。
- 增加标签归组领域模型和单元测试。

## 0.1.3

- 项目集合视图增加“添加项目”按钮。
- 支持添加当前窗口打开的文件夹或已保存 `.code-workspace`。
- 支持选择其他文件夹或 `.code-workspace` 添加到当前集合。
- 没有活动集合时，引导用户创建模板或打开已有集合。
- 自动生成相对于集合文件的便携路径，拒绝跨文件系统或跨 Windows 盘符写入。
- 添加时检查重复项目和重复别名，并收集可选说明与标签。
- 写入前显示项目条目预览，确认后才修改集合文件。
- 通过最小化文本编辑保留原有 JSONC 注释、排版与尾随逗号。

## 0.1.2

- 项目集合视图增加“创建项目集合模板”按钮和空状态入口。
- 模板包含字段用途、相对路径、项目类型和可选字段的简体中文注释。
- 集合解析器支持 JSONC 行注释与块注释，字符串中的 URL 不会被误处理。
- 创建模板时检测同名文件并拒绝覆盖已有内容。
- 普通点击项目默认询问在新窗口还是当前窗口打开。
- `projectManager.projectCatalog.openMode` 增加 `prompt` 模式并设为默认值。

## 0.1.1

- 隐藏只能由项目树条目调用的内部打开命令，避免命令面板出现无目标入口。
- 为项目打开命令增加缺少条目参数时的安全提示。

## 0.1.0

- 增加 `*.project-butler.json` 便携项目集合格式和 JSON Schema。
- 增加“项目管家”活动栏入口与项目集合树视图。
- 支持以别名展示文件夹和 `.code-workspace` 项目。
- 支持点击项目按默认方式打开，以及右键选择当前窗口或新窗口打开。
- 项目路径相对于集合文件解析，拒绝本机绝对路径。
- 增加集合文件自动识别、内容热更新、最近集合恢复和手动刷新。
- 无效配置和失效路径保留在项目树中，并通过警告、悬停说明和编辑器诊断反馈。

## 0.0.3

- 屏蔽父目录时自动合并其下的重复子规则。
- 已存在父目录屏蔽时，不再重复加入子项。
- 增加“管理/取消屏蔽”命令，支持单选和多选恢复。
- 取消父目录屏蔽时，恢复此前被父规则合并的子规则。
- 明确标签装饰颜色只影响文字或图标前景，不改变完整标签背景。

## 0.0.2

- 增加可重复执行的 VSIX 打包脚本。
- 增加外部文件归属诊断和更醒目的可见反馈。
- 修正本地安装包清单，排除源码、测试夹具和开发需求文档。

## 0.0.1

- 建立 TypeScript VS Code 插件基础工程。
- 支持在资源管理器中单选或多选资源，并同时写入目录展示、搜索和文件监控屏蔽规则。
- 支持按多根工作区分别应用屏蔽设置。
- 支持使用标签装饰和状态栏提醒识别工作区外文件。
- 增加基础配置、日志、单元测试和手工测试说明。
- 调试时自动打开隔离测试工作区，并增加外部文件诊断命令、醒目状态栏和编辑器标题警告图标。
